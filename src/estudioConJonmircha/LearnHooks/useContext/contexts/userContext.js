import { createContext } from 'react' // create context is a function that returns an object with two properties: Provider and Consumer

const UserContext = createContext()

export default UserContext